apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki-local
    url: http://loki:3100
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      maxLines: 1000
      timeout: 60
